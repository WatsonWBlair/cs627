# Minimal requirements for ablation studies with pre-generated tokens
# ~90% smaller dependency footprint than full requirements.txt

# Core ML (CPU-only for cost efficiency)
torch==2.5.1
torchvision==0.20.1
--index-url https://download.pytorch.org/whl/cpu

# Essential data handling
numpy>=1.21.0,<2.0
h5py>=3.8.0,<4.0

# Progress bars and utilities
tqdm>=4.65.0,<5.0

# Optional: for advanced ablation configs
click>=8.1.0,<9.0  # For CLI tool
pandas>=1.3.0,<2.0  # For results analysis